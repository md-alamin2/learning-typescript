// tuple type -> Tuple type is a special type of array that allows us to define the type of elements at specific positions in the array.

let user : [number, string];

user = [1, "jon"];
// user = ["jon", 1] // Error

console.log(user)